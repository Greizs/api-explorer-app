services:
  - type: web
    name: api-explorer-backend
    runtime: node
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 4000
      - key: EXTERNAL_API_URL
        value: https://jsonplaceholder.typicode.com